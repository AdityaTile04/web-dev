//! 1.getElementById
// Returns the element as an object or null value (if not found)

// Example 1
let imgObj = document.getElementById("mainImg");
console.log(imgObj);

// Example 2
let paraObj = document.getElementById("description");
console.log(paraObj);
